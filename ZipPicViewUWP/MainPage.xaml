<Page x:Name="page"
    x:Class="ZipPicViewUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZipPicViewUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:behaviors="using:Microsoft.Toolkit.Uwp.UI.Animations.Behaviors"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d" Loaded="PageLoaded" SizeChanged="PageSizeChanged"
    KeyUp="PageKeyUp">
    <RelativePanel x:Name="displayPanel" AllowDrop="True" DragOver="DisplayPanel_DragOver" Drop="DisplayPanel_Drop">
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" >
            <Grid x:Name="printPanel" Opacity="0" />
            <SplitView x:Name="splitView" DisplayMode="CompactOverlay" PanePlacement="Left" CompactPaneLength="50" OpenPaneLength="300">
                <SplitView.Content>
                    <controls:AdaptiveGridView x:Name="thumbnailGrid" DesiredWidth="200" ItemHeight="200" />
                </SplitView.Content>
                <SplitView.Pane>
                    <Grid Background="{StaticResource SidebarBackground}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Grid.Row="0">
                            <Button x:Name="subFolderButton" Width="50" Height="50" Background="Transparent" Click="SubFolderButtonClick">
                                <SymbolIcon Symbol="List" />
                            </Button>
                            <TextBlock VerticalAlignment="Center" Margin="0,15">Folders</TextBlock>
                        </StackPanel>
                        <ScrollViewer Grid.Row="1">
                            <ListView x:Name="subFolderListCtrl" SelectionChanged="SubFolderListSelectionChanged"
                                      DataFetchSize="2" Margin="-10,0,0,0" />
                        </ScrollViewer>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="2">
                            <controls:RadialProgressBar x:Name="thumbProgress"  Height="30" Width="30" HorizontalAlignment="Left"
                                                        VerticalAlignment="Center" Margin="10,10,10,10" />
                            <TextBlock x:Name="thumbProgressText" Text="Idle" Margin="10,10,10,10" VerticalAlignment="Bottom" />
                        </StackPanel>
                    </Grid>
                </SplitView.Pane>
            </SplitView>
            <interactivity:Interaction.Behaviors>
                <behaviors:Blur x:Name="BlurBehavior" Duration="500" AutomaticallyStart="False" />
            </interactivity:Interaction.Behaviors>
        </Grid>
        <RelativePanel x:Name="viewerPanel" Visibility="Collapsed" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" >
            <Border x:Name="imageBorder" Visibility="Collapsed" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True">
                <Image x:Name="image">
                    <interactivity:Interaction.Behaviors>
                        <behaviors:Blur x:Name="ImageTransitionBehavior" Duration="500" AutomaticallyStart="False" />
                    </interactivity:Interaction.Behaviors>
                </Image>
            </Border>

            <Border
                x:Name="hiddenImageControl"
                Visibility="Collapsed"
                ManipulationMode="TranslateX"
                ManipulationCompleted="HiddenImageControlManipulationCompleted"
                Tapped="HiddenImageControlTapped"
                Background="#00000000" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" />

            <local:ViewerControl
                x:Name="imageControl" Visibility="Collapsed"
                CloseButtonClick="ImageControlCloseButtonClick"
                NextButtonClick="ImageControlNextButtonClick"
                PreviousButtonClick="ImageControlPrevButtonClick"
                SaveButtonClick="ImageControlSaveButtonClick"
                PrintButtonClick="ImageControlPrintButtonClick"
                CopyButtonClick="ImageControlCopyButtonClick"
                OnAutoAdvance="ImageControlOnAutoAdvance" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" />

            <Border x:Name="loadingBorder" Visibility="Collapsed" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True">
                <ProgressRing IsActive="True" Width="100" Height="100" />
            </Border>
        </RelativePanel>
        <Grid RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" >
            <controls:InAppNotification x:Name="inAppNotification" />
        </Grid>
    </RelativePanel>
    <Page.TopAppBar>
        <CommandBar VerticalContentAlignment="Center" VerticalAlignment="Center">
            <CommandBar.Content>
                <TextBlock Margin="10,0,0,0" x:Name="filenameTextBlock" Text="&lt;None&gt;" UseLayoutRounding="True" />
            </CommandBar.Content>
            <AppBarToggleButton x:Name="fullscreenButton" Icon="FullScreen" Label="FullScreen" Checked="FullscreenButton_Checked"
                                Unchecked="FullscreenButtonUnchecked" />
            <AppBarSeparator />
            <AppBarButton x:Name="openFileButton" Icon="OpenFile" Label="Open File" Click="OpenFileButtonClick" />
            <AppBarButton x:Name="openFolderButton" Icon="Folder" Label="Open Folder" Click="OpenFolderButtonClick" />
            <AppBarSeparator />
            <AppBarButton x:Name="aboutButton" Icon="Help" Label="About" Click="AboutButtonClick" />
        </CommandBar>
    </Page.TopAppBar>
</Page>
